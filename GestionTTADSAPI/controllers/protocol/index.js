const {consultProtocol} = require("./utils/consultProtocol.protocol");
const {createProtocol} = require("./utils/createProtocol.protocol");
const {deleteProtocol} = require("./utils/deleteProtocol.protocol");
const {updateProtocol} = require("./utils/updateProtocol.protocol");
const {uploadPDF} =  require("./utils/uploadPDF.protocolo.js");

module.exports = {

    consultProtocol,
    createProtocol,
    deleteProtocol,
    updateProtocol,
    uploadPDF

}


   